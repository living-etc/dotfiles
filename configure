#! /bin/bash

for file in '.inputrc' '.bash_profile' '.bashrc' '.vimrc' '.gitconfig' '.tmux.conf' '.gemrc' '.rspec' '.gitmessage'; do
  if [ -f ~/$file ];
  then
    rm ~/$file
  fi
  ln -sf `pwd`/$file ~/$file;
done
